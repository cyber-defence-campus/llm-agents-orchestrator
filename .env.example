# Agent Orchestrator - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# LLM Configuration
# =============================================================================

# Model to use (litellm format: provider/model)
AGENT_MODEL=gemini/gemini-3-flash-preview

# API Keys (at least one required based on AGENT_MODEL)
OPENAI_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=

# Optional: Custom LLM API base URL (for local models or proxies)
# LLM_API_BASE=http://localhost:8080/v1

# =============================================================================
# Service Configuration
# =============================================================================

# Redis connection (default: redis for Docker, localhost for local)
REDIS_HOST=redis

# Port configuration
AGENT_MANAGER_PORT=8083
SANDBOX_PORT=8000

# =============================================================================
# Optional: Sandbox Configuration
# =============================================================================

# URL of sandbox service (only needed if using sandbox-runtime)
# AGENT_SANDBOX_URL=http://sandbox-service:8000

# =============================================================================
# Optional: Prompt Extension
# =============================================================================

# Additional paths to load prompts from (colon-separated)
# AGENT_PROMPT_PATHS=/app/prompts:/app/external-prompts
